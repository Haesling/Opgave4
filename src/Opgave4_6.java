public class Opgave4_6 {

    public static void main(String[] args) {

        System.out.println("Enter the radius og the bounding circle: ");
         int radius = 40;
         double α = Math.random() * 2 * Math.PI;
         double x = radius * Math.cos(α);
         double y = radius * Math.sin(α);

        System.out.println( x + "," + y);

            α = Math.random() * 2 * Math.PI;
            x = radius * Math.cos(α);
            y = radius * Math.sin(α);

        System.out.println( x + "," + y);

            α = Math.random() * 2 * Math.PI;
            x = radius * Math.cos(α);
            y = radius * Math.sin(α);

        System.out.println( + x + "," + y);

    }
}
